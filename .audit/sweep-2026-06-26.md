# Drift sweep — 2026-06-26

*HEAD: c9fad93 (#51), branch `main` (clean tree). Method: multi-agent dimensional finders (7 dimensions) + adversarial per-finding verification (default-refute, both sides re-confirmed independently). **17 candidates surfaced → 15 confirmed → 2 filtered as false positives.** One verifier (`code-reality-1`) hit the structured-output retry cap and was dropped by the harness; it was **re-verified by hand** and reinstated (see Notes). Per CLAUDE.md §7 this is a **catalog** — the AI does not auto-fix; the founder routes.*

## Executive summary

Doc health is **good**: **no critical findings**, and most drift is the expected residue of a fast-shipping milestone train (M2.2 → M2.3 → M2.4-lean → M2.9 down-payment) where the **forward-looking and navigation docs lagged behind the code and the per-topic owner docs**. The drift clusters in two places: (1) **stale "where are we / what's next" pointers** — the MEMORY.md Snapshot, the README Status line, and the ROADMAP Current-state block all still describe a pre-M2.9 world, each contradicting its own sibling sections or journal; and (2) **BLUEPRINT §4** (the agent-loop walkthrough), whose three sub-claims (service-identity mechanism, streaming mechanism, statelessness) are out of step with the same document's own §4.1/§5/§8.1 and with the module code. The single most important thing to route is **cross-doc-1 + cross-doc-2 together**: re-refresh the MEMORY.md Snapshot and the README Status line to ground truth (branch `main`, M2.9 down-payment shipped, next = M2.9.2), since those are the first things a fresh session and a new reader respectively read, and both are concretely false today. Everything else is a `[nice]` wording/count/altitude fix that can be batched into the next same-commit doc touch.

## Findings by severity

### Critical

_None._

### Important

**cross-doc-1** — [contradiction] — *MEMORY.md Snapshot names a stale active branch and "recent merges" list*
- **Locations:** `MEMORY.md:14` (Snapshot "Last refreshed: 2026-06-23"), `:95` (Active branch), `:96` (recent merges end at #49), `:1358-1373` (2026-06-24 Journal: "main clean, #50 merged"); `ROADMAP.md:32-41`; git HEAD c9fad93 (#51), clean.
- **Claim vs reality:** Snapshot asserts active branch `feat/m2.9-downpayment-ot008` with recent merges ending at M2.4 lean (#49). Reality: branch is `main`, clean at HEAD c9fad93 (#51); #50 and #51 have both merged — and MEMORY.md's own Journal one entry below already says so. The Snapshot was never re-refreshed in the #51 session-close-sweep commit.
- **Suggested correction:** Re-refresh the Snapshot: `MEMORY.md:14` → "*Last refreshed: 2026-06-24.*"; `:95` → "**Active branch:** `main` (clean at HEAD c9fad93, #51). Recent merges: … M2.4 lean (#49); M2.9 down-payment + OT-008 (#50); DEC-040 local-first + session-close sweep (#51)."

**cross-doc-2** — [drift] — *README "Status" line frozen at M2.5, names "Next: M2.2" (four merged milestones behind)*
- **Locations:** `README.md:27-31` (Status "… `M2.5` … Next: `M2.2`"); `ROADMAP.md:25/28/32/32-41`; `CLAUDE.md:9` §9; `MEMORY.md:85-114`.
- **Claim vs reality:** README (the "Start here" nav/status doc) says status ends at M2.5 and the next chunk is M2.2 agent presence/typing. Reality: M2.2 shipped (#41); M2.3 (#42), M2.4-lean (#49), and the M2.9 down-payment (#50) shipped after it. Every other doc lists the real next build as M2.9.2 (native Google sign-in). "Next: M2.2" is concretely false.
- **Suggested correction:** Update `README.md:27-31`: append the shipped M2.2/M2.3/M2.4-lean/M2.9-down-payment milestones and change "Next: `M2.2`…" to "Next: `M2.9.2` native Google sign-in + module aud guard." to match `ROADMAP.md:32`.

**blueprint-code-2** — [contradiction] — *BLUEPRINT §4 says the orchestrator connects via "OIDC client-credentials service identity"; code + §8.1 use a self-issued anonymous persisted token*
- **Locations:** `BLUEPRINT.md` §4 step 1 (lines 178-181) + §4 diagram label (line 29), §8.1 table (line 363), §4.1 (line 273); `services/orchestrator/src/spacetime.ts:1-4,39-52`; `CLAUDE.md` §9 (M1.5 "keeps its persisted-token identity … OT-007").
- **Claim vs reality:** §4 step 1 claims a stable Identity via an OIDC client-credentials **service account**. Reality: the orchestrator connects with a **self-issued anonymous token cached to a local file** (DEC-017) — stable Identity, but not an OIDC service account. The same doc's §8.1 and §4.1 already record this correctly and list the real service grant as the not-yet-done OT-007.
- **Suggested correction:** In `BLUEPRINT.md` §4 step 1 (178-179) and the §4 diagram label (29), mark the OIDC client-credentials service identity as the **target/OT-007 state** and note the orchestrator currently connects via a self-issued anonymous persisted token (DEC-017), aligning §4 with §8.1 (363) and §4.1 (273).

### Nice

**code-reality-1** — [drift] — *§9 says the gateway "spans 16 providers" but `PROVIDER_CATALOG` has 17* — **(re-verified by hand; automated verifier failed)**
- **Locations:** `CLAUDE.md:320` ("spans **16 providers**"); `CLAUDE.md:311-318` (the three-tier enumeration); `packages/shared/src/index.ts:74-137` (PROVIDER_CATALOG).
- **Claim vs reality:** §9 line 320 summarizes the gateway as spanning **16 providers**. The actual `PROVIDER_CATALOG` has **17 entries**: 13 single-API-key cloud (anthropic, openai, google, mistral, cohere, groq, xai, deepseek, perplexity, togetherai, fireworks, deepinfra, cerebras) + `openai-compatible`/local + 3 multi-credential (amazon-bedrock, azure, google-vertex). §9's own prose at 311-318 enumerates all three tiers (= 17), so the "16" summary undercounts by one (it omits the local/openai-compatible entry from the tally).
- **Suggested correction:** `CLAUDE.md:320` → "spans **17 providers** (13 single-API-key cloud + 1 local/openai-compatible + 3 multi-credential), all from one `PROVIDER_CATALOG`".

**code-reality-2** — [contradiction] — *§9 simultaneously says `agent_reply_append` "is kept for back-compat" (M1.6 para) and "is deleted" (M2.1 para); the reducer no longer exists*
- **Locations:** `CLAUDE.md:335-337` (M1.6), `:406` (M2.1); `modules/spacetime/src/index.ts:588` (stale comment), `:291-780`.
- **Claim vs reality:** M1.6 prose says present-tense "is kept for back-compat, deleted next milestone"; M2.1 prose says "is deleted." There is no `agent_reply_append` reducer in the module — only a stale comment at index.ts:588 referencing its past existence. So line 406 is correct and line 336 is the stale one.
- **Suggested correction:** Edit `CLAUDE.md:336` to past tense, e.g. "(`agent_reply_append` — the dormant old cumulative-UPDATE path — was kept for back-compat through M1.x and removed in M2.1.)", and delete/refresh the now-stale comment at `modules/spacetime/src/index.ts:588`.

**cross-doc-3** — [drift] — *CLAUDE §9 states "16 gateway tests" but the gateway has 22*
- **Locations:** `CLAUDE.md:326`; `ROADMAP.md:272` ("22 gateway"), `MEMORY.md:69/1190`; `packages/gateway/src/*.test.ts` (actual = 22).
- **Claim vs reality:** §9 (the code-reality doc that "wins for code facts") asserts "16 gateway tests"; the suite is now 22 (credentials 9 + gateway 7 + index 2 + providers 4). This is the M1.4-era count never bumped after M1.8 added provider/catalog tests; ROADMAP and MEMORY already cite 22.
- **Suggested correction:** `CLAUDE.md:326` → "22 gateway tests cover the BYOK crypto + stream normalization + per-provider/catalog coverage".

**orphans-1** — [orphan] — *SETUP.md S-6 binding-sync step points at a nonexistent "orchestrator's copy" of the bindings*
- **Locations:** `SETUP.md:203-209` (S-6 step 3); `BLUEPRINT.md:79-84` (§2); `services/orchestrator/package.json:21`; the three real binding dirs.
- **Claim vs reality:** S-6 step 3 says to sync bindings to "apps/mobile/module_bindings, packages/stdb-bindings, and the orchestrator's copy." Reality: the orchestrator has **no local copy** — it imports the workspace package `@agentspace/stdb-bindings` (`workspace:*`), as BLUEPRINT §2 states. The three real surfaces are `modules/spacetime/bindings` (canonical), `packages/stdb-bindings/src` (the imported package), and `apps/mobile/module_bindings` (vendored copy). The step both invents a fourth surface and omits the canonical one.
- **Suggested correction:** `SETUP.md:206-208` — replace "apps/mobile/module_bindings, packages/stdb-bindings, and the orchestrator's copy" with "modules/spacetime/bindings (canonical generated source), packages/stdb-bindings/src (the workspace package the orchestrator imports), and apps/mobile/module_bindings (the vendored copy)".

**stale-promises-1** — [stale-promise] — *ROADMAP "Current state" still points "Next: M2.9" though the M2.9 down-payment already shipped*
- **Locations:** `ROADMAP.md:32-33` (Next: M2.9), `:35` (self-contradicting "M2.9 STARTED"), `:368-373` (M2.9.3 "✓ [down-payment 2026-06-23]"); `MEMORY.md:110-114`; git #50.
- **Claim vs reality:** The Current-state block presents M2.9 as the upcoming not-yet-started milestone, while the same file (M2.9.3 "✓ down-payment") and MEMORY record it as shipped. The genuinely-pending work is the M2.9.2 chunk (native Google sign-in + module aud/iss guard, gated on S-9); the block even contradicts itself two lines later with "M2.9 STARTED" (line 35).
- **Suggested correction:** `ROADMAP.md:32-33` → "Next: M2.9.2 (native Google sign-in + module aud/iss guard; gated on S-9) → M3 (RAG)", matching the MEMORY.md Snapshot.

**stale-promises-2** — [drift] — *ROADMAP "Current state" date stamp (2026-06-23) is stale after the 2026-06-26 sweep edited the same file*
- **Locations:** `ROADMAP.md:14` (date "*2026-06-23.*"), `:438-442` (§5 BL-025/DEC-040 block added by #51); git c9fad93 (#51, 2026-06-26).
- **Claim vs reality:** The Current-state header is dated 2026-06-23, but #51 (2026-06-26) edited ROADMAP.md (added the DEC-040/BL-025 strategic-skip entry to §5) while leaving the Current-state block and its date untouched — so the sequencing summary lags the most recent doc-bearing session by 3 days.
- **Suggested correction:** Bump `ROADMAP.md:16` from "*2026-06-23.*" to "*2026-06-26.*" and add a one-line Current-state note that a local-first per-device DB was recorded as a strategic skip (DEC-040 / BL-025 / §5); do **not** claim new shipped code (#51 was doc-only).

**gaps-1** — [gap] — *CLAUDE.md §9 mobile file-tree omits `src/chat.ts` (the most-imported mobile module) and `src/config.ts`*
- **Locations:** `CLAUDE.md:256`; `apps/mobile/src/chat.ts:1`, `apps/mobile/src/config.ts:1`; `CLAUDE.md:502` (config.ts referenced in prose only).
- **Claim vs reality:** The mobile file tree lists auth/byok/reconnect + components but omits `chat.ts` and `config.ts`. `chat.ts` is the shared presentation/design-system layer (colors, shortId, fmtTime, relativeTime, initials) imported by all 12 mobile screens+components — the single most-imported mobile module — yet appears nowhere in §9. `config.ts` (connection target + auth config flags) is named in the M2.9 prose (line 502) but missing from the tree.
- **Suggested correction:** `CLAUDE.md:256` — add both modules: "… `src/byok.ts` · `src/chat.ts` · `src/config.ts` · `src/reconnect.tsx` …".

**gaps-2** — [gap] — *Reducers `set_display_name`, `create_group`, `add_member`, `leave_thread` are not named in CLAUDE.md §9 (only their features are)*
- **Locations:** `modules/spacetime/src/index.ts:291/324/507/724`; `CLAUDE.md` §9 (M1.3 prose); `SPEC.md:281-307`; `BLUEPRINT.md:103-123`.
- **Claim vs reality:** §9's M1.3 prose narrates the features ("New chat → create_dm … group Add member … add/remove/rename/leave") but never names four real reducers backing them: `set_display_name`, `create_group`, `add_member`, `leave_thread` (grep count = 0 in CLAUDE.md). SPEC/BLUEPRINT don't claim an exhaustive reducer enumeration, so this is a thin naming omission, not a missing-feature gap.
- **Suggested correction:** In `CLAUDE.md` M1.3 prose (around line 366), name `set_display_name`, `create_group`, `add_member`, and `leave_thread` alongside the already-listed sibling reducers.

**spec-module-1** — [drift] — *SPEC §2 says a failed run sets `runs.error`, but the `run` table has no `error` column*
- **Locations:** `SPEC.md` §2 (line 73); `modules/spacetime/src/index.ts:100-124` (run schema), `:686-694` (finish), `:719-720` (cancel).
- **Claim vs reality:** SPEC §2 states for `failed`: "unrecoverable error; `runs.error` set; message → `failed`." The `run` table declares no `error` column; the human-readable error text is written onto `message.text` (the streaming message row). No `.error` write or column exists on `run`.
- **Suggested correction:** `SPEC.md:73` → "unrecoverable error; failure text written onto the message row; message → `failed`" (drop the nonexistent `runs.error` reference).

**blueprint-code-1** — [drift] — *BLUEPRINT §4 step 5 still describes streaming as "batched row UPDATEs (~50ms)"; code (and §5) use append-only `reply_delta` INSERTs*
- **Locations:** `BLUEPRINT.md` §4 step 5 (lines 189-191) vs §5 (lines 301-318); `modules/spacetime/src/index.ts:126-148` (reply_delta), `:633-644` (begin), `:652-666` (delta INSERT); `CLAUDE.md` §9 (M1.9/DEC-030).
- **Claim vs reality:** §4 step 5 says reply text streams via "batched row UPDATEs (~50ms; DEC-008/OT-004)." Reality (M1.9/DEC-030): append-only constant-size `reply_delta` INSERTs; the message row stays `streaming` with empty text and takes exactly two writes (empty begin + final finish). The "~50ms batched cumulative UPDATE" was the **old O(n²) model** that caused the OT-004 tail-drop and was explicitly replaced. BLUEPRINT's own §5 already documents the new model, so §4 contradicts §5 of the same doc.
- **Suggested correction:** Rewrite `BLUEPRINT.md` §4 step 5 to: "Stream the reply into a private `reply_delta` table via append-only, constant-size INSERTs (one per ~100ms coalescing window; M1.9/DEC-030/OT-004); the message row stays `streaming` with empty text until `agent_reply_finish` writes the authoritative final text once (`complete`) and GCs the deltas — see §5," replacing the stale "batched row UPDATEs (~50ms…; DEC-008)" wording.

**blueprint-code-3** — [contradiction] — *BLUEPRINT §4 calls the orchestrator "stateless per request"; §4.1 + code describe a persistent, stateful, long-running subscriber*
- **Locations:** `BLUEPRINT.md` §4 (lines 193-194) vs §4.1 (lines 265-269); `services/orchestrator/src/supervise.ts` (long-running connect→onReady→await-disconnect loop), `src/byok.ts` (persisted in-memory NaCl keypair).
- **Claim vs reality:** §4 says the orchestrator is "stateless per request … so it can scale horizontally; run-claiming prevents double-processing." Reality: a persistent, stateful, single subscriber — open WS, in-memory NaCl keypair, in-flight Map/Set loop guard, coalescing batchers, file-persisted token; central-by-design (singleton `service`, one agent-member identity). No horizontal scaling / run-claiming today. The same doc's §4.1 says exactly this; the "stateless" line is aspirational reading as current.
- **Suggested correction:** In `BLUEPRINT.md` §4 (193-194), retag the sentence as forward-looking and defer to §4.1, e.g. "**Future (post-v1):** make the orchestrator stateless per request so it can scale horizontally with run-claiming; **v1 is a single stateful always-on process — see §4.1 / DEC-027.**"

**spec-module-3** — [drift] *(low confidence)* — *SPEC §2 "Idempotency contract" names a composite run key + claim-fails-if-claimed that the code replaces with `runId` + `agent_turn`*
- **Locations:** `SPEC.md` §2 (lines 76-80); `modules/spacetime/src/index.ts:599` (Duplicate runId guard), `:609-611` (agent_turn once-per-episode guard), `:252-265` (agent_turn table).
- **Claim vs reality:** SPEC §2's Idempotency contract gives the example key `thread_id + message_id + agent_id` with "claiming … fails if already claimed." Reality: runs are keyed by a client-owned `runId` (`agent_reply_begin` throws on a duplicate `runId`); the turn-level idempotency that bounds a duplicate agent turn is the `agent_turn` `(episodeId, agentId)` composite. SPEC's later "Implemented"/"M2.1 mechanics" notes already describe the runId/agent_turn reality, so the contract paragraph's specific key example is stale/illustrative — confusing but reconciled downstream (hence low confidence).
- **Suggested correction:** `SPEC.md:77-78` → "A run is uniquely keyed by a client-owned `runId`; idempotency of an agent's turn is the `agent_turn` `(episode_id, agent_id)` composite. Claiming (`agent_reply_begin`) is a conditional reducer that throws (creates no row) if the runId already exists or the agent already took its episode turn."

## Routing table

| ID | Type | Severity | One-line | Suggested correction |
|----|------|----------|----------|----------------------|
| cross-doc-1 | contradiction | **important** | MEMORY.md Snapshot names stale branch + merge list; contradicts its own Journal | Re-refresh Snapshot to `main`@c9fad93 (#51); date 2026-06-24; merges through #51 |
| cross-doc-2 | drift | **important** | README Status frozen at M2.5, "Next: M2.2" (4 milestones behind) | Append M2.2/2.3/2.4-lean/2.9-down-payment; "Next: M2.9.2" |
| blueprint-code-2 | contradiction | **important** | §4 claims OIDC service identity; code + §8.1 use self-issued anon token | Mark OIDC service identity as target/OT-007; note DEC-017 anon token is current |
| code-reality-1 | drift | nice | §9 says "16 providers"; PROVIDER_CATALOG has 17 | `CLAUDE.md:320` → "spans 17 providers (13 cloud + 1 local + 3 multi)" |
| code-reality-2 | contradiction | nice | §9 says `agent_reply_append` both "kept" and "deleted"; reducer gone | Make M1.6 line past-tense; refresh stale comment at index.ts:588 |
| cross-doc-3 | drift | nice | §9 says "16 gateway tests"; suite is 22 | `CLAUDE.md:326` → "22 gateway tests …" |
| orphans-1 | orphan | nice | SETUP S-6 syncs to nonexistent "orchestrator's copy" of bindings | Replace with the 3 real surfaces (modules/spacetime/bindings, packages/stdb-bindings/src, apps/mobile/module_bindings) |
| stale-promises-1 | stale-promise | nice | ROADMAP "Next: M2.9" but down-payment already shipped | "Next: M2.9.2 (native Google sign-in + aud/iss guard; gated on S-9)" |
| stale-promises-2 | drift | nice | ROADMAP Current-state date 2026-06-23 stale after #51 edited file | Bump date to 2026-06-26; note DEC-040/BL-025 local-first skip; no new code claim |
| gaps-1 | gap | nice | §9 mobile tree omits `chat.ts` (most-imported) + `config.ts` | Add both to `CLAUDE.md:256` |
| gaps-2 | gap | nice | 4 reducers (set_display_name/create_group/add_member/leave_thread) unnamed in §9 | Name them in M1.3 prose (~line 366) |
| spec-module-1 | drift | nice | SPEC §2 says failed run sets `runs.error`; no such column | `SPEC.md:73` → "failure text written onto the message row" |
| blueprint-code-1 | drift | nice | §4 step 5 says "batched row UPDATEs (~50ms)"; code uses reply_delta INSERTs | Rewrite §4 step 5 to the append-only INSERT model; ref §5/M1.9/DEC-030 |
| blueprint-code-3 | contradiction | nice | §4 calls orchestrator "stateless per request"; §4.1 + code say stateful | Retag sentence as forward-looking; defer to §4.1/DEC-027 |
| spec-module-3 | drift | nice *(low conf)* | SPEC §2 idempotency key example (thread+msg+agent) ≠ runId/agent_turn code | Update example to runId + `agent_turn (episode_id, agent_id)` |

## False positives filtered

**2 candidates** were surfaced by the finders and rejected under adversarial verification:

- **code-reality-3** — "§9 cites four different orchestrator-test counts (24 / 14 / 35 / 49)." All four quotes were confirmed verbatim and the suite was independently counted at 49 (prompt 35 + byok 5 + replyLoop 4 + supervise 3 + index 2). **Rejected:** §9 is an explicitly milestone-prefixed narrative (M1.6/M1.7/M2.1/M2.3); each "+N orchestrator tests" is a per-milestone *delta*, not a competing claim about the present total. The only clause phrased as a current aggregate — M2.3's "(49 orchestrator tests)" — is correct. No false statement exists; at most a readability nit, which does not meet the doc-vs-code-disagreement bar.

- **spec-module-2** — "SPEC §2's run state machine has a `queued` state + 'claim' transition the module never implements." All facts confirmed: SPEC diagrams `queued ──claim──▶ running`; the module inserts runs directly as `running`; `queued` appears only in a status comment. **Rejected:** SPEC is **prescriptive by charter** (CLAUDE §1), and the queued→claim lifecycle is the intended at-least-once trigger contract for the general run model (scheduled reducers are explicitly at-least-once). Decisively, SPEC §2 **self-reconciles inline** — its "Implemented (M1.9/DEC-030)" and M2.1 notes state a reply run is `running` from `agent_reply_begin` and a rejected trigger "never enters `queued`/`running`." A prescriptive doc that annotates its own implemented-subset delta is working as designed, not drifting.

## Notes

- **Dropped-then-reinstated finding.** During the Verify phase, one verifier (`code-reality-1`, the "16 vs 17 providers" finding) hit the harness's StructuredOutput retry cap (5) and produced no verdict, so it was filtered out of the automated synthesis. It was **re-verified by hand** this session — `PROVIDER_CATALOG` in `packages/shared/src/index.ts:74-137` has 17 entries (13 `apiKey` + 1 `baseUrl`/local + 3 `multi`) vs the "16 providers" claim at `CLAUDE.md:320` — confirmed REAL (nice) and reinstated above. The corrected tally is **17 candidates → 15 confirmed → 2 filtered**.
- **Verification depth.** Findings were confirmed against the code at HEAD c9fad93; cited line numbers should be re-confirmed at edit time, since several cited docs (CLAUDE.md, ROADMAP.md, MEMORY.md) are long and a same-commit correction may shift surrounding lines.
- **Two-sided fixes (CLAUDE §8 "coupled features are one feature in two files").** `code-reality-2` and `blueprint-code-1` each have a code-side companion to the doc edit: `code-reality-2` should also refresh the stale source comment at `modules/spacetime/src/index.ts:588`; `blueprint-code-1`'s doc edit must keep BLUEPRINT §4 and §5 consistent.
- **Clustering — route as one batch.** cross-doc-1 / cross-doc-2 / stale-promises-1 / stale-promises-2 share **one root cause**: the #51 session-close sweep refreshed ROADMAP §5 but did not re-snapshot the three "where are we" surfaces (MEMORY Snapshot, README Status, ROADMAP Current-state). They can be fixed in a single follow-up commit.
- **`spec-module-3`** is **low confidence** — the SPEC paragraph is reconciled by its own downstream "Implemented" notes, so a reviewer may legitimately leave it as an illustrative general-model example. Routing it is optional.
- **Not re-derived this sweep:** test *counts* beyond the two cited (gateway=22, orchestrator=49) were taken as verified by the finders; if any doc edit touches those numbers, re-run `pnpm --filter <pkg> test` to reconfirm before committing.
